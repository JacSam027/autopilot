---
title: 微调模型部署与推理规范
scope: autopilot 内部使用，部署微调模型时必读
last_updated: 2026-08-13
---

# 微调模型部署与推理规范

本文件规定 autopilot 在部署 / 运行微调模型推理服务时必须遵守的操作规范。

## 核心原则

- **禁止污染全局环境**：必须使用 Docker 或新建 conda 环境，不允许在全局或项目现有环境中安装微调模型依赖。
- **环境可重建**：依赖配置应可重建（Dockerfile / docker-compose.yml / environment.yml 随代码提交），新环境能按配置一键拉起，避免"仅在一台机器上能跑"的隐式依赖。
- **资源受限运行**：推理服务应在资源受限的环境中运行（CPU / 内存 / GPU 限制），避免占满本地资源影响其他任务。
- **提示词越短越好，压缩关键规范**：训练 / 微调模型时，提示词（system prompt / instruction）应尽量简短，同时在短篇幅内**把尽可能多的关键规范压缩进去**，如输出格式、必须包含的字段、禁止行为等。不必要的约束（如冗长的风格要求、可从训练数据中学到的模式）应移除，让模型从数据中学习而非从提示词死记。
- **数据集实验隔离，每轮备份**：每次新增实验（新的一轮训练 / 微调），其训练集 / 评估集等数据必须**备份保留**。开启新一轮训练且需要修改训练集时，**必须复制副本，在副本上修改**，禁止直接修改原数据集。确保每轮实验都有专属的数据集版本，可追溯"用哪个数据集版本跑出了哪个结果"。
- **自主改良训练数据必须可逆**：当 AI 开始自主改良训练数据时（数据清洗、标注修正、样本过滤、增强等），**必须遵循可逆原则**——通过先备份数据再修改来实现。改良前必须保留原数据的完整副本，改良后的数据存为新版本；禁止直接修改原数据集。确保能随时回滚到改良前的状态，对比不同数据版本的效果。

## 环境隔离

### Docker 优先

推荐用 Docker 容器运行推理服务，确保依赖、端口、资源完全隔离。

- **容器配置**：编写 `Dockerfile` 或 `docker-compose.yml`，明确指定：
  - 基础镜像（通常与模型训练环境一致）
  - 依赖安装（模型框架、推理库）
  - 端口映射
  - 资源限制（`--cpus`、`--memory`、GPU 设备分配）
- **配置文件随代码提交**：Dockerfile / docker-compose.yml 必须提交到仓库，确保新环境能按配置重建。
- **运行方式**：通过 `docker run` 或 `docker-compose up` 启动推理服务，禁止在容器外直接安装推理依赖。

### conda 隔离（备选）

若本地有 conda，可新建专用环境，在该环境内安装依赖并运行服务。

- **新建环境**：`conda create -n ft-inference python=3.x`，使用独立的虚拟环境。
- **禁止污染现有环境**：不允许在项目现有环境或全局 `base` 环境中安装微调模型依赖。
- **环境可导出**：执行 `conda env export > environment.yml` 并提交到仓库，确保新环境能按配置重建。
- **激活方式**：运行推理服务前先激活环境（`conda activate ft-inference`），结束后可停用（`conda deactivate`）。

### 环境复用（可选）

如果现有环境能成功跑起推理服务，可以直接复用，但**严禁修改现有环境**。

- **可以复用**：项目现有环境、已有的 Docker 容器、或系统已有的 conda 环境，如果它们已经包含所需依赖且能成功运行推理服务，可以直接使用，无需新建。
- **禁止修改**：对复用的环境**不能做任何变更** —— 不安装新依赖、不改动配置、不调整环境变量、不升级框架版本。保持环境原样。
- **需要变更时的唯一路径**：如果现有环境缺少依赖或需要变更配置，**必须新建环境**（新 Docker 容器、新 conda 环境），在新环境里做变更并运行服务。禁止为了省事而修改现有环境。
- **保护原环境**：复用环境的目的是"不污染",确保原环境继续可供其他任务使用,不受本次部署影响。

## （后续章节占位）

后续将补充以下规范，章节待完善：

- **数据集管理**：实验隔离、副本复制、版本命名、可追溯性
- **提示词工程**：提示词压缩原则、格式规范设计、约束必要性判断
- **权重管理**：模型权重存储、版本管理、权限控制
- **监控告警**：推理服务监控、性能指标、异常告警
- **灰度策略**：多版本并行、流量切换、回滚机制
- **成本控制**：资源使用优化、按需启停、成本追踪
- **安全边界**：鉴权、限流、数据脱敏、合规要求

## 推理服务注意事项

### 渠道切换与上下文长度

切换不同 LLM 渠道进行打标/推理时，必须考虑上下文长度限制：

- **thinking 模式消耗 token**：带 thinking 模式的渠道（如 deepseek-v4）会在输出前消耗大量 token
- **max_tokens 设置考虑**：必须为 thinking 留足空间，否则 text 段会被截空（返回空字符串）
- **空返回误判风险**：空返回会被误判为"模型判断未评价"→ 假阴性暴增

**验证步骤**：
1. 小批量测试（50-100 条）使用**真实长 prompt**（不能用简化版）
2. 统计空返回率，确保≈0
3. 检查 evaluated 率是否在预期范围
4. 验证通过后才能全量部署

**关键原则**：短 prompt 单测有盲区，必须在真实长 prompt 上验证

---

## autopilot 何时读本文件

当任务涉及以下操作时，必须先读本文件：
- 部署微调模型的推理服务
- 安装或配置模型推理依赖
- 启动或管理推理服务进程
- 调整推理服务的资源配置
