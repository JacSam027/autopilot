# 测试账号模板

> **复制本文件为 `accounts.local.md`（同目录），再填入真实账号。**
> `accounts.local.md` 已被 `.gitignore` 忽略，不会被提交。
> 供 `/autopilot` 在 Verify 阶段用 `playwright-skill` 登录测试。**自动汇报时不要把密码写进报告。**

## 普通用户
- 登录页 URL:
- 用户名:
- 密码:

## 超级管理员（如有）
- 登录页 URL:
- 用户名:
- 密码:

## 备注
- 测试数据 / 初始化说明:
- 其他注意事项:
